[package]
name = "resq-plugin-sdk"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "SDK for out-of-process RESQ-kit plugins: manifest, JSON-RPC framing, stdio transport"
repository = "https://github.com/unite-q3-team/resq-plugin-sdk"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
